# Copy to deploy/docker/.env (or export env vars) and fill values

# --- API auth ---
RAG_API_KEY=change-me
RAG_API_PORT=8000

# --- OpenAI-compatible LLM/Embedding ---
LLM_BINDING_API_KEY=your_api_key
LLM_BINDING_HOST=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIM=1536

# --- PostgreSQL container ---
POSTGRES_USER=rag_user
POSTGRES_PASSWORD=rag_password
POSTGRES_DB=rag_db
POSTGRES_PORT_EXPOSED=5432

# --- LightRAG storage selection (enable to test PG backend) ---
# For local filesystem backend, leave these empty.
# For PostgreSQL backend, set these:
# LIGHTRAG_KV_STORAGE=PGKVStorage
# LIGHTRAG_VECTOR_STORAGE=PGVectorStorage
# LIGHTRAG_DOC_STATUS_STORAGE=PGDocStatusStorage

# (Optional) override POSTGRES_HOST/POSTGRES_PORT for app container
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
